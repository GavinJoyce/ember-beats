var App = require('app');

App.{{#camelize}}{{name}}{{/camelize}}Controller = Em.Controller.extend({

});


App.{{#camelize}}{{name}}{{/camelize}}View = Em.View.extend({
	didInsertElement: function(){
	}
});